# Vite 8 совместим с @vitejs/plugin-react@4.x на практике, но peer-deps плагина
# пока не обновлены. Разрешаем lenient peer-dep resolution.
# Когда выйдет @vitejs/plugin-react@5+ с явной поддержкой Vite 8 — этот файл
# можно удалить.
legacy-peer-deps=true
