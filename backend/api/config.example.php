<?php
/**
 * Шаблон конфигурации. Скопируйте в config.php и заполните своими значениями.
 * Файл config.php добавлен в .gitignore — не коммитьте свои креды.
 */
return [
    'db' => [
        'host'    => '127.0.0.1',
        'port'    => 3306,
        'name'    => 'graph_from_data',
        'user'    => 'gfd_user',
        'pass'    => 'CHANGE_ME',
        'charset' => 'utf8mb4',
    ],

    // Откуда фронтенд будет ходить в API (для CORS).
    // Можно перечислить несколько доменов через запятую, или '*' для разработки.
    'cors_allowed_origins' => 'https://db.zweb.kz',

    // Максимальный размер загружаемого Excel (в байтах). По умолчанию 20 МБ.
    'max_upload_size' => 20 * 1024 * 1024,

    // Папка для временного хранения загруженных файлов
    'uploads_dir' => __DIR__ . '/../uploads',

    // Время жизни сессии (секунд). 8 часов.
    'session_lifetime' => 8 * 3600,
];
